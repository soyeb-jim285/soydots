# Unified package list — installed via yay (handles repo + AUR transparently).

# --- base toolchain ---
base-devel
git
jq
zip
unzip
wl-clipboard
gum

# --- shell + prompt ---
zsh
zsh-autosuggestions
zsh-syntax-highlighting
zsh-completions
starship
zoxide
eza
bat
fzf

# --- terminal + editor ---
kitty
neovim
tmux

# --- fonts ---
# Maple Mono NF is bundled under fonts/maple-mono and installed by 50-post.sh.
noto-fonts-emoji

# --- wayland / hyprland stack ---
hyprland
hyprlock
hypridle
hyprsunset
hyprshot
qt6-wayland
xdg-desktop-portal-hyprland
uwsm

# --- shell ui / quickshell deps ---
quickshell
python-gobject
python-pillow

# --- quill-polkit build + runtime deps ---
cmake
polkit-qt6
hyprland-qt-support

# --- display manager ---
greetd
greetd-tuigreet
terminus-font

# --- audio ---
pipewire
pipewire-alsa
pipewire-pulse
wireplumber

# --- bluetooth ---
bluez
bluez-utils

# --- screen / brightness / misc ---
brightnessctl
ddcutil
grim
slurp
swappy
cliphist
fuzzel
upower
plocate

# --- theming (qt/gtk) ---
qt6ct
kvantum

# --- monitoring ---
btop

# --- nvidia (installed only if nvidia machine; harmless on others) ---
linux-headers

# --- hyprfm (file manager) + optional runtime deps ---
# hyprfm-git is an AUR package; yay handles it transparently.
hyprfm-git
gvfs
gvfs-smb
ffmpeg
poppler
perl-image-exiftool
udisks2
# wl-clipboard and bat are already listed above.

# --- flatpak (flathub remote is added by 50-post.sh) ---
flatpak
xdg-desktop-portal
xdg-desktop-portal-hyprland
xdg-desktop-portal-gtk

# --- AUR ---
dracula-cursors-git
zen-browser-bin
kvantum-theme-catppuccin
awww
