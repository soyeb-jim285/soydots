# Unified package list — installed via yay (handles repo + AUR transparently).

# --- base toolchain ---
base-devel
git
jq
zip
unzip
wl-clipboard
gum

# --- shell + prompt ---
zsh
# zsh plugins are git-cloned by ~/.config/zsh/plugins.zsh (radley-style):
# zsh-autosuggestions, zsh-history-substring-search, zsh-vi-mode, fast-syntax-highlighting
starship
zoxide
eza
bat
fzf

# --- terminal + editor ---
kitty
neovim
tmux

# --- fonts ---
# Maple Mono NF is bundled under fonts/maple-mono and installed by 50-post.sh.
noto-fonts-emoji

# --- wayland / hyprland stack ---
hyprland
hyprlock
hypridle
hyprsunset
hyprshot
qt6-wayland
xdg-desktop-portal-hyprland
uwsm

# --- shell ui / quickshell deps ---
quickshell
python-gobject
python-pillow

# --- quill-polkit build + runtime deps ---
cmake
polkit-qt6
hyprland-qt-support

# --- display manager ---
greetd
greetd-tuigreet
terminus-font

# --- audio ---
pipewire
pipewire-alsa
pipewire-pulse
wireplumber

# --- bluetooth ---
bluez
bluez-utils

# --- screen / brightness / misc ---
brightnessctl
ddcutil
grim
slurp
swappy
cliphist
fuzzel
upower
plocate

# --- theming (qt/gtk) ---
qt6ct
kvantum
# Papirus icons so GTK apps and file-chooser portals match catppuccin.
# (The filechooser portal interface is served by xdg-desktop-portal-gtk for
# ALL apps, Qt included — so Qt file dialogs are GTK and need icons too.)
# The GTK theme itself is catppuccin-gtk-theme-mocha in the AUR section below.
papirus-icon-theme

# --- graphics (AMD iGPU acceleration; keeps load off the flaky NVIDIA dGPU) ---
# vulkan-radeon gives the AMD iGPU a Vulkan device — without it the ONLY Vulkan
# device is the dGPU, so any gpu-api=vulkan app (e.g. mpv) lands on the dGPU.
vulkan-radeon
libva-utils

# --- media ---
# mpv config + plugins (uosc/thumbfast/autoload/sponsorblock) vendored in repo
# under mpv/, symlinked to ~/.config/mpv. yt-dlp powers streaming + sponsorblock.
mpv
yt-dlp

# --- monitoring ---
btop

# --- nvidia support ---
# Driver packages are installed by setup-nvidia.sh only on NVIDIA machines.
linux-headers

# --- hyprfm (file manager) + optional runtime deps ---
# hyprfm-git is an AUR package; yay handles it transparently.
hyprfm-git
gvfs
gvfs-smb
ffmpeg
poppler
perl-image-exiftool
udisks2
# wl-clipboard and bat are already listed above.

# --- flatpak (flathub remote is added by 50-post.sh) ---
flatpak
xdg-desktop-portal
xdg-desktop-portal-hyprland
xdg-desktop-portal-gtk

# --- AUR ---
dracula-cursors-git
# GTK theme for GTK apps + file-chooser portals (lavender variant selected in
# gtk settings.ini / gsettings). kvantum (Qt) theme is instead vendored in repo
# under Kvantum/ — works offline via the symlink, no AUR package needed for it.
catppuccin-gtk-theme-mocha
awww
