import QtQuick
import QtQuick.Layouts
import ".."

ColumnLayout {
    spacing: Theme.spacingLg

    Text {
        text: "Display components will appear here as they are built."
        color: Theme.textTertiary
        font.pixelSize: Theme.fontSize
        font.family: Theme.fontFamily
    }
}
